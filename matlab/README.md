# matlab/

## What goes here

`ipc_sensitivity_model.m` — analytical IPC-vs-reservation-station-count
and IPC-vs-CDB-width sensitivity model, owned by the Python/MATLAB Lead.

Validate this model against your team's measured simulator data (§L) —
the required plot overlays the analytical model on measured points.
